appId: com.rsoar.memo
productName: memo
directories:
  output: release
  buildResources: build
files:
  - out/**
  - resources/**
  - package.json
# Native modules can't be loaded from inside the asar archive.
asarUnpack:
  - "**/*.node"
linux:
  target:
    - AppImage
  category: Utility
  icon: build/icon.png
  artifactName: ${productName}-${version}.${ext}
